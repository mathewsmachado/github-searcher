import { ReactNode } from 'react';

export type {{pascalCase name}}Props = {
  children: ReactNode;
  color?: 'primary' | 'accent';
};

export type WrapperProps = Exclude<{{pascalCase name}}Props, 'children'>;
